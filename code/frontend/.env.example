# Copy to .env and fill in for local development. Never commit the real .env.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/movie_app"
NEXTAUTH_SECRET="replace-with-a-random-32-byte-secret"
NEXTAUTH_URL="http://localhost:3000"

# Seeded once by prisma/seed.ts. Change the password after first login.
FOUNDER_EMAIL="founder@example.com"
FOUNDER_PASSWORD="change-me-locally"
